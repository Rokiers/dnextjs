# 应用配置
NODE_ENV=development
PORT=4000

# 数据库配置
DATABASE_URL="postgresql://postgres:password@postgres:5432/ecommerce?schema=public"

# JWT 配置
# 重要：JWT_SECRET 必须至少 32 个字符以确保安全性
# 生产环境请使用强随机密钥，可以使用以下命令生成：
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-key-at-least-32-characters-long-change-in-production
JWT_EXPIRES_IN=7d

# CORS 配置
CORS_ORIGIN=http://localhost:3000
